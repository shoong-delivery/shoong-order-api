version: '3.8'

services:
  order-service:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      PORT: 3001
      DATABASE_URL: ${DATABASE_URL}
      KITCHEN_URL: ${KITCHEN_URL}
      NOTIFICATION_URL: ${NOTIFICATION_URL}
